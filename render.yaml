services:
  - type: web
    name: ipsco-django-app
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: gestion_vehicules.settings
      - key: DATABASE_URL
        fromDatabase:
          name: ipsco-postgres
          property: connectionString
      - key: SECRET_KEY
        value: "django-insecure-ipsco-deploy-2025-08-17-secret-key-for-production-deployment"
      - key: DEBUG
        value: false
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://ipsco-django-app.onrender.com"

databases:
  - name: ipsco-postgres
    databaseName: ipsco_db
    user: ipsco_user
